================ PYTHON BASICS ROADMAP ================

1. VARIABLES & DATA TYPES
   int         → 10
   float       → 3.14
   complex     → 2 + 3j
   str         → "Hello"
   bool        → True, False
   list        → [1, 2, 3]
   tuple       → (1, 2, 3)
   set         → {1, 2, 3}
   dict        → {"name": "Esha"}
   NoneType    → None

2. TYPE CONVERSION
   int(), float(), str(), list(), tuple(), set(), dict(), bool()

3. OPERATORS
   Arithmetic  → +, -, *, /, //, %, **
   Comparison → ==, !=, >, <, >=, <=
   Logical    → and, or, not
   Assignment → =, +=, -=, *=, /=
   Membership → in, not in
   Identity   → is, is not

4. INPUT & OUTPUT
   input()
   print()
   f-strings

5. CONDITIONAL STATEMENTS
   if
   elif
   else

6. LOOPS
   for
   while
   range()

7. LOOP CONTROL STATEMENTS
   break
   continue
   pass

8. STRINGS
   Indexing
   Slicing
   len()
   upper()
   lower()
   replace()
   split()
   join()
   strip()
   find()
   count()
   startswith()
   endswith()

9. LISTS
   append()
   extend()
   insert()
   remove()
   pop()
   clear()
   index()
   count()
   sort()
   reverse()
   copy()
   List Comprehension

10. TUPLES
    count()
    index()
    Packing & Unpacking

11. SETS
    add()
    remove()
    discard()
    pop()
    clear()
    union()
    intersection()
    difference()
    symmetric_difference()
    issubset()
    issuperset()

12. DICTIONARIES
    keys()
    values()
    items()
    get()
    update()
    pop()
    popitem()
    clear()
    Dictionary Comprehension

13. FUNCTIONS
    def
    return
    Parameters & Arguments
    Default Arguments
    Keyword Arguments
    *args
    **kwargs
    Recursion

14. VARIABLE SCOPE
    Local Variables
    Global Variables
    LEGB Rule
    global keyword
    nonlocal keyword

15. LAMBDA FUNCTIONS
    lambda arguments: expression

16. HIGHER ORDER FUNCTIONS
    map()
    filter()
    reduce()
    zip()
    enumerate()

17. EXCEPTION HANDLING
    try
    except
    else
    finally
    raise
    Custom Exceptions

18. FILE HANDLING
    open()
    read()
    readline()
    readlines()
    write()
    writelines()
    close()
    with statement

    File Modes:
    r, w, a, x, rb, wb, ab

19. MODULES
    import
    from ... import
    as keyword
    Built-in Modules
    Custom Modules

20. PACKAGES
    __init__.py
    Package Structure

21. OBJECT ORIENTED PROGRAMMING (VERY IMPORTANT)
    Class
    Object
    Constructor (__init__)
    Instance Variables
    Class Variables
    Instance Methods
    Class Methods
    Static Methods

22. FOUR PILLARS OF OOP
    Encapsulation
    Inheritance
    Polymorphism
    Abstraction

23. SPECIAL (DUNDER) METHODS
    __str__()
    __repr__()
    __len__()
    __add__()

24. ITERATORS
    iter()
    next()
    Iterator Protocol

25. GENERATORS
    yield
    Generator Expressions

26. DECORATORS
    Function Decorators
    @decorator syntax

27. COMPREHENSIONS
    List Comprehension
    Dictionary Comprehension
    Set Comprehension

28. VIRTUAL ENVIRONMENT
    python -m venv venv
    venv\Scripts\activate

29. PIP
    pip install package_name
    pip uninstall package_name
    pip list
    pip freeze

30. IMPORTANT BUILT-IN MODULES
    math
    random
    os
    sys
    datetime
    time
    json
    collections
    itertools
    pathlib
    shutil

31. JSON HANDLING
    json.loads()
    json.dumps()
    json.load()
    json.dump()

32. REGULAR EXPRESSIONS (IMPORTANT)
    import re
    re.search()
    re.match()
    re.findall()
    re.sub()

33. API HANDLING (IMPORTANT)
    requests.get()
    requests.post()
    Response Status Codes
    JSON Response Handling

34. DEBUGGING
    print()
    breakpoint()
    Reading Tracebacks

35. CODING STANDARDS
    PEP 8
    Meaningful Variable Names
    Proper Indentation
    Comments
    Docstrings

36. GIT & GITHUB BASICS
    git init
    git status
    git add .
    git commit -m "message"
    git push
    git pull
    git clone

37. PYTHON INTERVIEW TOPICS
    Mutable vs Immutable
    List vs Tuple
    Set vs Dictionary
    is vs ==
    Deep Copy vs Shallow Copy
    *args vs **kwargs
    Iterator vs Generator
    Method Overriding
    Method Overloading
    Class Method vs Static Method
    MRO
    GIL
    PEP 8
    Exception Handling

38. MUST-DO PRACTICE PROGRAMS
    Prime Number
    Palindrome
    Factorial
    Fibonacci Series
    Armstrong Number
    Anagram Check
    Pattern Printing
    Sorting Algorithms
    Searching Algorithms
    Frequency Counter
    Student Management System
    Bank Management System
    File Handling Projects

================ JOB READY PYTHON CHECKLIST ================

□ Python Syntax
□ Data Types
□ Operators
□ Conditional Statements
□ Loops
□ Strings
□ Lists
□ Tuples
□ Sets
□ Dictionaries
□ Functions
□ Exception Handling
□ File Handling
□ Modules & Packages
□ OOP
□ Iterators & Generators
□ Decorators
□ JSON
□ Regular Expressions
□ APIs (requests)
□ Virtual Environment
□ pip
□ Git & GitHub
□ Interview Questions
□ Practice Projects

